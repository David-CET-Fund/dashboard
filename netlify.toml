[dev]
  command = "npm run dev"
  publish = "dist"
  port = 8888 

[build]
  command = "npm run build" 
  publish = "dist" 

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/hello"
  to = "/.netlify/functions/hello"
  status = 200